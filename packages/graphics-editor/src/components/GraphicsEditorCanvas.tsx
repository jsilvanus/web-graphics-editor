import type { FC, PointerEvent as ReactPointerEvent, RefObject, WheelEvent as ReactWheelEvent } from "react";
import { WIDTH, HEIGHT } from "../constants";
import type { Layer, PathNode } from "../types";
import { CanvasLayer } from "./canvas/CanvasLayer";
import { SelectionOverlay } from "./canvas/SelectionOverlay";
export interface DrawingPreview{tool:"line"|"path"|"orthogonal";points:Array<{x:number;y:number}>}
export const GraphicsEditorCanvas:FC<{artboardRef:RefObject<HTMLDivElement|null>;layers:Layer[];selectedIds:Set<string>;grid:boolean;safe:boolean;background:string;zoom?:number;panX?:number;panY?:number;viewportRef?:RefObject<HTMLDivElement|null>;onWheel?: (event:ReactWheelEvent)=>void;onViewportPointerDown?:(event:ReactPointerEvent)=>void;onViewportPointerMove?:(event:ReactPointerEvent)=>void;onViewportPointerUp?:()=>void;onFit?:()=>void;onZoomIn?:()=>void;onZoomOut?:()=>void;onPointerMove:(event:ReactPointerEvent)=>void;onPointerUp:()=>void;onCanvasPointerDown:(event:ReactPointerEvent)=>void;onLayerPointerDown:(event:ReactPointerEvent,id:string,kind:"move"|"resize"|"rotate",handle?:string)=>void;onPathNodes?:(id:string,nodes:PathNode[])=>void;drawing?:DrawingPreview|null;onDrawingDoubleClick?:()=>void}>=p=>{const{artboardRef,layers,selectedIds,grid,safe,background,zoom=1,panX=0,panY=0,viewportRef,onWheel,onViewportPointerDown,onViewportPointerMove,onViewportPointerUp,onFit,onZoomIn,onZoomOut,onPointerMove,onPointerUp,onCanvasPointerDown,onLayerPointerDown,onPathNodes,drawing}=p;return <div className="ge-canvas-wrap"><div className="ge-viewport" ref={viewportRef} onWheel={onWheel} onPointerDown={onViewportPointerDown} onPointerMove={onViewportPointerMove} onPointerUp={onViewportPointerUp} style={{position:"relative",overflow:"hidden",touchAction:"none"}}><div className="ge-viewport-controls" style={{position:"absolute",right:8,top:8,zIndex:1000,display:"flex",gap:4}}><button onClick={onZoomOut} type="button">−</button><span style={{padding:"4px 7px",background:"#222",color:"#fff",fontSize:12}}>{Math.round(zoom*100)}%</span><button onClick={onZoomIn} type="button">+</button><button onClick={onFit} type="button">Fit</button></div><div className="ge-canvas" onPointerMove={onPointerMove} onPointerUp={onPointerUp} onPointerLeave={onPointerUp}><div ref={artboardRef} className="ge-artboard" style={{background,transform:`translate(${panX}px,${panY}px) scale(${zoom})`,transformOrigin:"0 0"}}>{grid&&<div className="ge-grid"/>}<div className="ge-guide ge-guide-v"/><div className="ge-guide ge-guide-h"/>{layers.map(layer=><CanvasLayer key={layer.id} layer={layer} selected={selectedIds.has(layer.id)} multiSelected={selectedIds.size>1} onPointerDown={(event,kind,handle)=>onLayerPointerDown(event,layer.id,kind,handle)} onNodes={nodes=>onPathNodes?.(layer.id,nodes)}/>)}{drawing&&drawing.points.length>0&&<svg className="ge-drawing-preview" width="100%" height="100%" viewBox={`0 0 ${WIDTH} ${HEIGHT}`} style={{position:"absolute",inset:0,pointerEvents:"none",zIndex:200}}><polyline points={drawing.points.map(p=>`${p.x},${p.y}`).join(" ")} fill="none" stroke="#38bdf8" strokeWidth="4" strokeDasharray="8 6"/>{drawing.points.map((p,i)=><circle key={i} cx={p.x} cy={p.y} r="6" fill="#fff" stroke="#38bdf8" strokeWidth="3"/>)}</svg>}{<SelectionOverlay layers={layers} selectedIds={selectedIds} onPointerDown={(event,kind,handle)=>{const id=[...selectedIds][0];if(id)onLayerPointerDown(event,id,kind,handle)}}/>}{safe&&<><div className="ge-safe safe90"/><div className="ge-safe safe80"/></>}</div></div></div></div>};
export const canvasAspectRatio=`${WIDTH}/${HEIGHT}`;
