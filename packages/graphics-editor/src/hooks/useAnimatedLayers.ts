import { useMemo } from "react";
import { interpolateKeyframes, transitionType } from "../timeline";
import type { GraphicsDocument, SceneTimeline, Layer } from "../types";

function transitionStyle(type:string,progress:number){if(progress<=0)return{};switch(type){case"slide-left":return{transform:`translateX(${-100*progress}%)`};case"slide-right":return{transform:`translateX(${100*progress}%)`};case"slide-up":return{transform:`translateY(${-100*progress}%)`};case"slide-down":return{transform:`translateY(${100*progress}%)`};case"dissolve":return{opacity:Math.max(0,1-progress),filter:`blur(${progress*2}px)`};case"fade":return{opacity:Math.max(0,1-progress)};default:return{}}}
export function useAnimatedLayers(document:GraphicsDocument,timeline:SceneTimeline){return useMemo(()=>{const active=timeline.scenes.find(s=>timeline.currentTime>=s.start&&timeline.currentTime<s.start+s.duration)??timeline.scenes.at(-1);const tr=active?.transition;const progress=tr&&tr.duration>0?Math.max(0,Math.min(1,(timeline.currentTime-(active!.start+active!.duration-tr.duration))/tr.duration)):0;return document.layers.map(layer=>{let next={...layer};for(const track of timeline.tracks.filter(t=>t.layerId===layer.id)){const value=interpolateKeyframes(track.keyframes,timeline.currentTime);if(value===undefined)continue;if(track.property==="opacity")next.style={...next.style,opacity:value};else if(track.property==="rotation")next.rotation=value;else(next as any)[track.property]=value}const clip=timeline.clips?.find(c=>c.layerId===layer.id);if(clip&&!(timeline.currentTime>=clip.start&&timeline.currentTime<clip.start+clip.duration))next.style={...next.style,opacity:0};if(tr&&progress>0)next.style={...next.style,...transitionStyle(transitionType(tr.type),progress)};return next})},[document.layers,timeline]);}
